# SP500 / E1R 项目每日日志

**日期：** 2026-07-19
**项目：** `FD-M3180125 Engine Canonical Regime Integration / Forward Runtime`
**Engine：** `FD-M3180125-SP500-TOP3-engine`
**工作分支：** `main`
**当日结束状态：** MG-Step 1/2/3 已完成；FW-Step 2 最终接口确认已通过；FW-Step 3 尚未开始
**下一工作日唯一入口：** `FW_STEP3_READ_ONLY_EXECUTION_PREFLIGHT_AND_POST_2026_06_16_DAILY_RUN_PLANNING`

---

## 1. 今日目标

今日工作的目标限定为两项：

1. 恢复正式 5Y 回测实际使用的 Market State / Market Gate 配置，修正 Engine 内错误的默认合同，并完成 MG-Step 1/2/3 等价验证。
2. 在不进入 Forward 正式执行的前提下，完成 FW-Step 2 的最终接口与数据流确认。

今日明确未进行：

```text
FW-Step 3 Router 分支执行
HOLD / ADD / REDUCE / EXIT / BUY
OrderIntent 生成
T+1 Fill
Pending Order 状态推进
Forward Account 更新
Daily Commit
Forward catch-up
Dashboard 集成
```

---

## 2. 工作计划边界

### 2.1 MG 工作线

固定为：

```text
MG-Step 1：Definition and Contract Freeze
MG-Step 2：Engine Integration
MG-Step 3：Testing and Validation
```

今日没有增加新的阶段、子阶段或旁支工作。

### 2.2 FW 工作线

固定为：

```text
FW-Step 1：正式 5Y 基线与 Forward 边界
FW-Step 2：Forward Seed / Shared Runtime / 最终接口确认
FW-Step 3：Read-only execution preflight、逐日运行规划及后续 Forward 执行验收
```

MG-Step 3 与 FW-Step 3 保持严格区分。

---

## 3. 正式 5Y Market Gate 机制恢复

### 3.1 Source of Truth

正式 Variant：

```text
E1R_REGIME_AWARE_V0_2_STATEFUL_MAX3
```

Canonical Strategy Commit：

```text
d7eb4dc0288433c68332e4607ae261d2f615d371
```

正式 Variant 的源码展开关系：

```text
**base
**_gate_g4
```

恢复出的正式 Market 配置：

```text
market_gate_enabled       = true
risk_off_below_spx_ma50   = false
market_shock_gate_enabled = false
market_shock_daily_return = -0.02
gate_use_slope            = true
gate_use_leadership       = true
```

正式 Gate Variant：

```text
D2_RISK_OFF_GATE
```

结论：

```text
正式 5Y 默认机制不启用 Shock。
```

---

## 4. 1259 日正式历史重放

正式 Artifact：

```text
exports/official/FD-M3180125-SP500-TOP3-engine/
backtest/canonical_5y/regular_eod_account_records.json
```

SHA256：

```text
0407a384311e5af7a1091355800c2afc2c95c7680ac16efb82544209438d8885
```

正式日期范围：

```text
2021-06-11 至 2026-06-16
```

重放结果：

```text
formal_date_count          = 1259
gate_mismatch_count        = 0
base_field_mismatch_count  = 0
```

Market State 重建分布：

```text
FULL_ON       = 660
CAUTIOUS_ON   = 134
CASH_MODE     = 465
```

Market Gate 分布：

```text
ALLOW         = 794
RISK_OFF      = 465
SHOCK         = 0
```

正式判定：

```text
PASS_MG_STEP3_FORMAL_1259_DAY_EQUIVALENCE
```

说明：

- 正式 5Y Artifact 没有直接保存三态 `Market State` 字段；
- Market State 使用正式源码公式、正式配置和相同市场数据重建；
- 重建后生成的 Market Gate 与正式 Artifact 1259 日逐日完全一致。

---

## 5. MG-Step 1 修正

此前 Engine 默认合同错误地使用：

```text
D3_RISK_OFF_PLUS_SHOCK_GATE
market_shock_gate_enabled = true
risk_off_below_spx_ma50   = true
```

今日修正为正式 5Y 合同：

```text
D2_RISK_OFF_GATE
market_shock_gate_enabled = false
risk_off_below_spx_ma50   = false
use_ma50_slope            = true
use_index_leadership      = true
```

修正文件：

```text
src/e1r_engine/market_state.py
src/e1r_engine/market_gate.py
```

可选 D3 Shock 能力仍保留，但必须显式 opt-in，不再作为默认合同。

MG-Step 1 状态：

```text
PASS
```

---

## 6. MG-Step 2 Engine 集成确认

`E1RCoreEngine` 已确认拥有并消费：

```text
MarketStateConfig
MarketGateConfig
MarketStateEvaluator
MarketGateEvaluator
```

Engine 当前正式配置：

```text
MarketStateConfig(
    market_gate_enabled=True,
    market_shock_gate_enabled=False,
    market_shock_daily_return=-0.02,
    use_ma50_slope=True,
    use_index_leadership=True
)

MarketGateConfig(
    variant='D2_RISK_OFF_GATE',
    market_gate_enabled=True,
    risk_off_below_spx_ma50=False,
    market_shock_gate_enabled=False,
    market_shock_daily_return=-0.02
)
```

MG-Step 2 状态：

```text
PASS
```

---

## 7. MG 测试合同迁移与 RCA

### 7.1 三次失败触发 RCA

修正过程中，旧测试仍固化 D3 / Shock 默认假设，连续三次导致验收中止。

正式 RCA：

```text
PRIMARY_ROOT_CAUSE:
未在修改正式默认合同前完成测试套件的全量旧 D3 合同盘点。

SECONDARY_ROOT_CAUSE:
使用测试函数名称作为 Shock 语义识别依据，
遗漏名称不含 shock 但仍断言旧 D3 行为的测试。

PROCESS_FAILURE:
采用逐个失败逐个修补，而不是先建立完整合同迁移矩阵。
```

### 7.2 只读合同审查

审查范围：

```text
12 个 MG 相关测试文件
60 个测试方法
```

锁定需要迁移的旧 D3 测试共 6 个：

```text
test_shock_boundary
test_shock（Market State / Gate Contract）
test_shock（Engine Integration）
test_market_gate_shock_precedence
test_engine_owns_canonical_evaluator_configs
test_engine_default_contract_is_canonical_max3_d3
```

迁移原则：

```text
正式默认合同测试：
验证 D2 No-Shock

可选 Shock 能力测试：
显式启用 D3 Shock
```

迁移后相关 MG、Engine 和 UPTREND 消费链测试全部通过。

---

## 8. MG 正式提交

提交：

```text
0741c5c6e4f0fcf23f1ca48c91625b3dfee06f99
Correct MG defaults to formal five-year contract
```

提交内容：

```text
9 files changed
350 insertions
77 deletions
```

新增：

```text
docs/architecture/FD-M3180125_MG_FORMAL_5Y_CORRECTION.json
docs/architecture/FD-M3180125_MG_FORMAL_5Y_CORRECTION.md
tests/test_e1r_formal_5y_market_config.py
```

保护状态：

```text
src/engine/backtest.py unchanged
protected legacy hash unchanged
HEAD == origin/main
working tree clean
```

MG 最终状态：

```text
MG-Step 1：PASS
MG-Step 2：PASS
MG-Step 3：PASS
```

---

## 9. 对 Forward 工作线的影响

MG 集成不修改：

```text
正式 5Y Artifact
正式 5Y 交易记录
Forward Seed 现金
Forward Seed 持仓数量
Forward Seed 成本
origin_branch
```

MG 集成影响的是后续 Forward Runtime 的正式判定来源：

```text
Market Data
→ Canonical Regime Generator
→ Market State Evaluator
→ Market Gate Evaluator
→ Daily Strategy Input
```

Forward 必须使用 Engine 内已经完成 1259 日历史等价验证的正式 MG 路径。

---

## 10. Forward 连续性原则重新确认

`SIM_END` 是 5Y 回测终点的强制结算动作，不属于策略本身。

Forward 连续运行必须：

```text
延续 2026-06-16 最后常规 EOD 的真实策略持仓
忽略回测专用 SIM_END 清算
不使用清算后的全现金账户
不把延续持仓记为新 BUY
从后续应处理交易日逐日连续推进
数据缺失时停止，不允许跳过日期
```

延续持仓：

```text
MRVL
DELL
HUM
```

它们保留：

```text
remaining shares
average cost
remaining cost basis
origin_branch = UPTREND
```

后续可执行：

```text
HOLD
ADD
REDUCE
EXIT
```

只有存在空余名额且策略条件允许时，才能执行新的 `BUY`。

---

## 11. FW-Step 2 原始边界恢复

今日一度把以下内容错误提前放入 FW-Step 2：

```text
逐日连续运行证明
首个 Forward 日期确认
Router 分支执行
订单和成交
账户更新
Forward catch-up
```

经重新核对，确认这些属于 FW-Step 3。

FW-Step 2 的正确终点是：

```text
Official Forward Seed
→ Market Data Adapter / Snapshot
→ Canonical Regime Generator
→ Market State Evaluator
→ Market Gate Evaluator
→ Daily Strategy Input
```

到此停止。

---

## 12. FW-Step 2 最终接口确认

执行：

```text
FD-M3180125 FW-STEP 2 FINAL INTERFACE CONFIRMATION V2
```

结果：

```text
PASS_FW_STEP2_FINAL_INTERFACE_CONFIRMATION
```

确认内容：

```text
Seed Contract：PASS
Shared Runtime Ownership：PASS
Engine Regime / Market State / Market Gate Path：PASS
Daily Strategy Input Boundary：PASS
```

正式 Seed 根目录：

```text
exports/official/FD-M3180125-SP500-TOP3-engine/
forward/seed_2026-06-18
```

Seed 合同确认：

```text
seed_date = 2026-06-18
continued_positions = MRVL / DELL / HUM
positions_are_new_buys = false
sim_end_liquidation_replayed = false
all_cash_final_settlement_used = false
unknown_pending_order_executable = false
```

Engine MG 配置确认：

```text
D2_RISK_OFF_GATE
Shock disabled
risk_off_below_spx_ma50 = false
MA50 slope enabled
leadership enabled
```

Forward 层确认：

```text
不重新定义 Canonical Regime Generator
不重新定义 Market State Evaluator
不重新定义 Market Gate Evaluator
通过 Engine 调用正式生成路径
```

### 12.1 FW-Step 2 停止边界

本次明确未执行：

```text
Regime Router 分支
HOLD / ADD / REDUCE / EXIT / BUY
OrderIntent
Fill
Pending Order transition
Account mutation
Daily state commit
Forward catch-up
```

执行后：

```text
source_modified = false
forward_state_written = false
commit_created = false
working_tree_clean = true
```

FW-Step 2 最终状态：

```text
COMPLETE
```

---

## 13. 今日输出目录

### 13.1 Canonical Commit Recovery

```text
/Users/dongfang/Downloads/
FD_M3180125_MG_CANONICAL_COMMIT_RECOVERY_V3_20260719T132816Z
```

### 13.2 MG-Step 1/2/3 修正与验证

```text
/Users/dongfang/Downloads/
FD_M3180125_MG_STEP123_CORRECTION_20260719T135218Z
```

### 13.3 MG 测试合同只读审查

```text
/Users/dongfang/Downloads/
FD_M3180125_MG_TEST_CONTRACT_REVIEW_20260719T134641Z
```

### 13.4 FW-Step 2 最终接口确认

```text
/Users/dongfang/Downloads/
FD_M3180125_FW_STEP2_FINAL_INTERFACE_CONFIRMATION_20260719T142956Z
```

---

## 14. 当日最终状态

```text
MG-Step 1：COMPLETE / PASS
MG-Step 2：COMPLETE / PASS
MG-Step 3：COMPLETE / PASS

FW-Step 1：COMPLETE
FW-Step 2：COMPLETE / PASS
FW-Step 3：NOT_STARTED
```

仓库状态：

```text
HEAD = 0741c5c6e4f0fcf23f1ca48c91625b3dfee06f99
HEAD == origin/main
working tree clean
```

正式判定：

```text
PASS_MG_STEP123_FORMAL_5Y_EQUIVALENCE
PASS_FW_STEP2_FINAL_INTERFACE_CONFIRMATION
```

---

## 15. 下一工作日唯一入口

明日只进入：

```text
FW-Step 3 Read-only execution preflight
and post-2026-06-16 daily run planning
```

FW-Step 3 首先应完成：

```text
1. 只读加载正式 Forward Seed；
2. 确认延续 MRVL / DELL / HUM；
3. 忽略 SIM_END 强制清算；
4. 确认 2026-06-16 后应处理的首个交易日；
5. 建立不跳日的完整逐日运行序列；
6. 核查每个日期所需市场与候选数据；
7. 数据缺失时停止，不允许跨日跳过；
8. 验证 Router 可到达正确 Regime 分支；
9. 预览 HOLD / ADD / REDUCE / EXIT / BUY；
10. 预览 OrderIntent、T+1 执行和账户变化；
11. 不写正式 Forward 状态，直至 Preflight 验收通过。
```

Preflight 通过后，才进入 FW-Step 3 后续正式 Forward catch-up、状态写入、幂等性和运行验收。

在明日授权前继续保持：

```text
OFFICIAL_FORWARD_EXECUTION_LOCKED
```

---

## 16. 收工结论

今日完成：

```text
正式 5Y Market State / Gate 机制恢复
Engine MG 默认合同修正
1259 日正式 Artifact 零差异验证
MG-Step 1/2/3 闭环
FW-Step 2 最终接口确认
```

今日未启动 FW-Step 3，也未写入任何正式 Forward 账户状态。

下一工作日从 FW-Step 3 只读执行预检开始。
