# SP500 / E1R 项目每日日志

**日期：** 2026-07-23
**项目：** `SP500趋势跟踪分析及交易辅助系统`
**Engine 正式名称：** `FD-M3180125-SP500-TOP3-engine`
**主开发仓库：** `/Users/dongfang/Downloads/sp500-tracker-v13`
**历史只读仓库：** `/Users/dongfang/Downloads/sp500-tracker-5y`
**GitHub：** `DonarFang/SP500-tracker`
**工作分支：** `main`
**当日最终 HEAD：** `1d3bf156e6dd674ea7278934cc39a8da547d42e2`
**HEAD == origin/main：** `TRUE`
**当日结束状态：** Engine Forward 独立 GitHub Daily 生产路径已启用；首次正式增量运行 PASS；Legacy Forward 与 Engine Forward 保持完全隔离；Dashboard 尚未接入
**下一观察点：** 等待下一次 Legacy 日更完成后，由 `workflow_run` 自动触发 `Engine Forward Daily`，确认 GitHub Actions 环境自动运行、提交和推送

---

# 0. 今日执行摘要

今天完成了 Engine Forward 从正式 Seed/Runtime 验收到 GitHub 独立 Daily 生产路径启用的完整闭环。

最终正式结果：

```text
PASS_FW_STEP3_FORMAL_CATCHUP_AND_RUNTIME_ACCEPTANCE
PASS_ENGINE_FORWARD_INDEPENDENT_GITHUB_DAILY
PASS_FW_STEP2_INDEPENDENT_ENGINE_DAILY_FIRST_RUN
FW_STEP2_GITHUB_DAILY_PRODUCTION=ACTIVE
```

截至今日结束：

```text
Canonical Seed date:
2026-06-16

First Engine Forward date:
2026-06-17

Initial formal catch-up last date:
2026-07-17

First independent daily incremental dates:
2026-07-20
2026-07-21
2026-07-22

Current Engine Forward last_committed_date:
2026-07-22

Engine Forward equity curve points:
24

Legacy workflow modified:
FALSE

Legacy Forward changed:
FALSE

Legacy equity curve changed:
FALSE

Dashboard integration performed:
FALSE
```

---

# 1. 今日新增并永久生效的开发规则

以下规则从今天开始属于 SP500 / E1R 项目的正式开发方式，适用于后续所有分析、排错、步骤设计、代码和脚本交付。

## 1.1 问题发生和下一步骤前必须回看历史

任何以下情况发生时：

```text
出现报错
运行结果与预期不一致
准备分析根因
准备提出下一步骤
准备改变计划
准备修改代码
准备生成新脚本
```

必须先完成：

```text
1. 回看相关对话历史；
2. 回看最近 3 天 Daily Log；
3. 回看相关正式交接、合同冻结和架构文件；
4. 区分：
   - FACT：已经证实
   - INFERENCE：由证据推导
   - UNKNOWN：尚未证实
5. 不得把已经证实的事实重新当成未知；
6. 不得重复已经完成的 RCA、审计或验证；
7. 若出现新证据与历史结论冲突，必须明确指出冲突后再行动。
```

正式规则：

> 每次有问题时，以及进行下一个步骤之前，必须先回看相关历史和最近 3 天的 Daily Log。

该规则的目的：

```text
避免重复分析
避免重复提问
避免重复审计
避免重复生成脚本
避免重新猜测已冻结事实
避免因忽略历史导致低级错误
```

## 1.2 得到确认后再输出代码

任何实现代码、补丁、Shell 脚本、GitHub workflow、配置或可执行命令输出之前，必须先向用户明确说明：

```text
目标
范围
不做什么
依赖的历史事实
预计修改文件
验收标准
失败保护
```

然后取得用户明确确认。

正式规则：

> 在得到用户确认后，才能输出代码或可执行脚本。

例外仅限：

```text
用户已经在同一条消息中明确授权，
且目标、范围和验收标准已经由历史冻结且没有歧义。
```

即使属于例外，也必须先回看历史，不得跳过历史核对。

## 1.3 其他继续有效的工程规则

```text
1. 冻结 legacy 策略文件，禁止修改。
2. 证据优先；UNKNOWN 不得编码。
3. 不新增阶段、子阶段、旁支审计或额外框架。
4. 同一目标失败 3 次，立即停止并做 RCA。
5. 不把环境问题混成代码问题。
6. Market Gate 属于高风险合同，必须 source-equivalent。
7. 全账户同时持股 <= 3。
8. Forward 层不得重写 Engine 策略或仓位管理逻辑。
9. 新脚本必须只有一个明确职责。
10. `git add` 必须使用显式白名单。
11. 失败恢复只删除本次创建路径，保留运行前已有 untracked 文件。
12. 发送脚本前必须完成：
    - bash syntax check
    - embedded Python compile check
    - heredoc 完整性
    - trailing whitespace scan
    - forbidden-operation scan
    - staging whitelist check
13. Git 同步使用：
    - git fetch
    - merge --ff-only
    不使用固定旧 HEAD 强行覆盖新历史。
14. 每次正式交付必须包含：
    - 编译/静态验证
    - 显式 add
    - commit
    - push
    - 最终 git status
    - HEAD / origin/main 对齐。
```

---

# 2. 当前权威总体计划

项目总体路线图固定为三步，不得扩展、改名或新增阶段。

## Step 1 — Canonical 5Y Official Artifact

状态：

```text
COMPLETE / PASS
```

已完成：

```text
正式 5Y Variant
UPTREND / SIDEWAYS 正式替换
Canonical Regime
Market State
Market Gate
Router
策略分支
排名
仓位
T+1
账户状态
正式交易记录
正式权益曲线
正式 Artifact
```

正式 Variant：

```text
E1R_REGIME_AWARE_V0_2_STATEFUL_MAX3
```

正式 Engine：

```text
FD-M3180125-SP500-TOP3-engine
```

## Step 2 — Shared Runtime + Forward / Live Tracks

状态：

```text
IN_PROGRESS，但 Engine Forward GitHub Daily 生产路径已 ACTIVE
```

Step 2 当前已经完成：

```text
Canonical Forward Seed
Shared Runtime
Forward Adapter
Canonical Regime / Market State / Market Gate consumption
Engine Daily Flow
Formal catch-up
Runtime acceptance
Independent GitHub Daily workflow
Independent Engine Forward equity curve
首次正式增量 daily production run
幂等重跑验证
Legacy/Engine 双系统隔离
```

尚待完成的最后运行事实：

```text
观察下一次真实 GitHub workflow_run 自动触发，
确认 GitHub Actions 环境自动完成：
数据更新
Engine Forward 运行
Engine official artifact commit
push
```

## Step 3 — Dashboard Manifest Integration and E2E Acceptance

状态：

```text
NOT_STARTED
```

Step 3 才允许进行：

```text
Dashboard manifest
Dashboard 数据源接入
Dashboard 页面展示 Engine Forward
Engine Forward equity curve 展示
完整 E2E
正式切换
移除 Legacy Forward 前测展示
移除 Legacy equity curve
Engine Forward 成为唯一正式系统
```

当前严禁提前进行 Dashboard 接入。

---

# 3. FW 工作线当前权威定义

历史中曾使用过不同的 FW-Step 编号定义，今天已按用户最终确认统一口径。

## FW-Step 1 — 正式基线、Seed 与 Runtime 合同

状态：

```text
COMPLETE
```

内容：

```text
正式 5Y 边界
Canonical Seed
Forward Runtime contract
Shared Runtime contract
Engine daily input boundary
```

## FW-Step 2 — GitHub 独立 Daily Forward 生产运行

目标：

```text
在 GitHub 上每天自动更新 Engine Forward 数据，
产生新的 Engine Forward daily records，
推进自身账户状态，
维护自身 orders/history，
延长自身独立 equity curve。
```

状态：

```text
PRODUCTION ACTIVE
```

已通过：

```text
独立 workflow 安装
独立 runner 安装
首次正式增量运行
幂等重跑
Legacy/Engine 隔离
commit / push
```

下一观察点：

```text
OBSERVE_NEXT_AUTOMATIC_GITHUB_DAILY_RUN
```

## FW-Step 3 — Dashboard Integration and E2E

目标：

```text
将完整 Engine Forward 系统接入 Dashboard，
在明确切换时移除 Legacy Forward 和 Legacy equity curve。
```

状态：

```text
NOT_STARTED
```

重要说明：

```text
此前日志中把 Read-only Preflight / Formal Catch-up 称为 FW-Step 3。
这些是当时的内部执行命名。

从今天起，按用户最终确认：
FW-Step 2 的完成目标包含 GitHub 每日独立更新；
FW-Step 3 专属于 Dashboard integration 和 E2E。
```

后续日志不得再混用这两个口径。

---

# 4. 正式文件管理架构

## 4.1 本地仓库

主仓库：

```text
/Users/dongfang/Downloads/sp500-tracker-v13
```

历史只读仓库：

```text
/Users/dongfang/Downloads/sp500-tracker-5y
```

GitHub：

```text
https://github.com/DonarFang/SP500-tracker
branch: main
```

## 4.2 Engine 源码

核心目录：

```text
src/e1r_engine/
```

关键模块：

```text
src/e1r_engine/core.py
src/e1r_engine/canonical_regime.py
src/e1r_engine/market_state.py
src/e1r_engine/market_gate.py
src/e1r_engine/regime_router.py
src/e1r_engine/forward_runtime.py
src/e1r_engine/forward_orchestrator.py
src/e1r_engine/forward_production_composition.py
src/e1r_engine/contracts.py
src/e1r_engine/state.py
src/e1r_engine/adapters/historical_data.py
```

冻结 Legacy 策略文件：

```text
src/engine/backtest.py
src/engine/e1r_composer.py
src/engine/e1r_sidecar_sleeve.py
```

## 4.3 Forward 数据

Engine Forward 专用数据：

```text
data/fw_prices/
```

上游行情来源：

```text
data/prices/
```

Engine Daily runner 负责同步：

```text
data/prices
→ data/fw_prices
```

禁止使用：

```text
data/oos/*
```

## 4.4 Official Engine Forward Namespace

根目录：

```text
exports/official/
FD-M3180125-SP500-TOP3-engine/
forward/
```

Canonical Seed：

```text
forward/seed_2026-06-16/
```

正式 Runtime：

```text
forward/runtime/
```

当前状态：

```text
forward/runtime/current/account_state.json
forward/runtime/current/runtime_state.json
forward/runtime/current/pending_orders.json
forward/runtime/current/manifest.json
```

逐日正式产物：

```text
forward/runtime/daily/YYYY-MM-DD/
  account_state.json
  decision_trace.json
  equity.json
  execution.json
  fills.json
  manifest.json
  order_intents.json
```

历史：

```text
forward/runtime/history/orders.jsonl
forward/runtime/history/equity_curve.json
```

自动化状态：

```text
forward/automation/current_run.json
```

## 4.5 GitHub Workflow

Legacy workflow：

```text
.github/workflows/update.yml
name: 每日更新 S&P 500 数据
```

Engine 独立 workflow：

```text
.github/workflows/engine-forward-daily.yml
name: Engine Forward Daily
```

Engine 独立运行入口：

```text
scripts/run_engine_forward_daily.py
```

---

# 5. 双系统隔离正式合同

在未来明确切换前，两套系统必须完全隔离。

## 5.1 Legacy GitHub Forward

继续拥有自己的：

```text
workflow
Forward 数据
账户状态
OOS 输出
equity curve
当前 Dashboard 数据源
```

## 5.2 Engine Forward

独立拥有自己的：

```text
data/fw_prices
runtime/current
runtime/daily
runtime/history/orders.jsonl
runtime/history/equity_curve.json
automation/current_run.json
```

## 5.3 禁止跨系统行为

Engine Forward 不得：

```text
执行或替换 run_oos.py
写 data/oos/*
写 exports/oos_*
写 exports/e1r_v0_2_*
覆盖 Legacy equity curve
向 Legacy equity curve 提供数据
拼接 Legacy equity curve
向当前 Dashboard 提供数据
使用 Legacy account/state/orders/equity 作为事实来源
```

Legacy Forward 不得向 Engine Forward提供：

```text
Seed
账户
订单
成交
权益
manifest
验证依据
开发证据
```

Git 同步不等于数据流。

## 5.4 未来正式切换

未来仅在 FW-Step 3 完成后：

```text
停止 Legacy Forward
移除 Legacy 前测数据
移除 Legacy equity curve
Dashboard 整体切换到 Engine Forward
Engine Forward 成为唯一正式系统
```

当前尚未执行该切换。

---

# 6. 今日问题、分析、结论与决定

## 6.1 Formal Catch-up V1 错误：误判 Adapter 日期职责

错误：

```text
STOP: unexpected initial runtime dates:
('2023-05-16', '2023-05-17', '2023-05-18')
```

错误原因：

```text
ProductionForwardDataAdapter 返回完整历史时间线是正确行为。
脚本错误地要求 data.trading_dates 从 2026-06-17 开始。
```

正确职责：

```text
Adapter:
加载完整数据历史

ForwardDatePlanner / Runner:
根据 Seed 与 last_committed_date 裁剪正式 Forward 日期
```

决定：

```text
仅修正脚本断言；
不修改 Adapter；
不修改 Date Planner；
不修改 Engine。
```

V2 结果：

```text
PASS_FW_STEP3_FORMAL_CATCHUP_AND_RUNTIME_ACCEPTANCE
```

## 6.2 初次误解：过早替换 Legacy Daily Forward

曾提出：

```text
Engine Forward 完全替换现有 GitHub daily forward update
并向现有 equity curve 提供数据
```

用户纠正：

```text
Legacy 与 Engine Forward 当前必须保持两套系统完全区隔。
Engine Forward 不向 Legacy equity curve 提供数据。
未来再整体移除 Legacy 并切换。
```

正式决定：

```text
停止 Takeover 方向；
不运行 Takeover V2；
改为新增独立 Engine Forward workflow。
```

## 6.3 独立 workflow V1 错误：猜错 Date Planner 接口

错误：

```text
TypeError:
plan() got an unexpected keyword argument 'seed_state'
```

原因：

```text
业务层直接猜测 ForwardDatePlanner.plan 参数，
没有使用历史既定的 Runner 上层接口。
```

正确接口：

```text
composition.runner.dry_run()
```

决定：

```text
只修正 Check 模式；
由 Runner 加载 Runtime state 和规划日期；
保留 dry-run 无写入合同。
```

## 6.4 独立 workflow 安装脚本收尾错误

执行结果：

```text
commit 99666bc2 已成功 push
```

但最后出现：

```text
STOP: tracked worktree not clean
data/fw_prices/*.json
```

原因：

```text
--check 模式执行了 data/prices → data/fw_prices 同步，
导致大量 tracked fw_prices 本地变化；
安装 commit 只提交了 4 个安装文件；
最终 clean check 因 fw_prices 变化停止。
```

事实：

```text
远端安装 commit 已成功；
本地失败恢复回退到旧 HEAD；
workflow 和 runner 已经存在于 origin/main；
正式 Engine 增量运行尚未发生。
```

决定：

```text
不重复安装；
下一脚本先 ff-only 同步至 origin/main；
然后执行第一次正式增量 daily run；
只提交 Engine 专属产物。
```

## 6.5 首次 Engine Daily 正式运行

输入状态：

```text
last_committed_date_before = 2026-07-17
```

规划日期：

```text
2026-07-20
2026-07-21
2026-07-22
```

正式执行：

```text
result_count = 3
committed_dates =
2026-07-20
2026-07-21
2026-07-22
```

结果：

```text
last_committed_date_after = 2026-07-22
final_account_date = 2026-07-22
final_equity = 278130.3458399169
Engine equity curve points = 24
Engine equity curve last date = 2026-07-22
```

幂等重跑：

```text
planned_dates = []
result_count = 0
committed_dates = []
runtime hash unchanged = PASS
```

隔离：

```text
Legacy workflow modified = FALSE
Legacy Forward changed = FALSE
Legacy equity curve changed = FALSE
Dashboard integration = FALSE
```

---

# 7. 今日 GitHub 操作历史

## 7.1 Engine Forward Contract Freeze

Commit：

```text
54aa2283171d0b5f6ff6ebfcceecea562268f8e4
```

Message：

```text
Freeze Engine Forward contracts from canonical 5Y evidence
```

结果：

```text
Canonical Seed contract frozen
Seed date = 2026-06-16
First Forward date = 2026-06-17
DELL / HUM / MRVL carried positions
pending orders = []
```

## 7.2 Formal Catch-up and Runtime Acceptance

Commit：

```text
8d1f244278068802d8e22bb43f90a07d5d21bf96
```

Message：

```text
Complete Engine Forward catch-up and runtime acceptance
```

提交内容：

```text
21 daily dates
2026-06-17 → 2026-07-17
153 official runtime artifacts
current state
daily records
orders history
Engine equity curve
runtime acceptance evidence
```

## 7.3 Independent Engine Forward Workflow Installation

Commit：

```text
99666bc2cf377479ab8ded4c6b2bc96ba648228e
```

Message：

```text
Add independent Engine Forward daily workflow
```

新增：

```text
.github/workflows/engine-forward-daily.yml
scripts/run_engine_forward_daily.py
docs/architecture/
  FD-M3180125_ENGINE_FORWARD_INDEPENDENT_GITHUB_DAILY_2026-07-23.md
  FD-M3180125_ENGINE_FORWARD_INDEPENDENT_GITHUB_DAILY_2026-07-23.json
```

## 7.4 First Independent Daily Production Run

Commit：

```text
1d3bf156e6dd674ea7278934cc39a8da547d42e2
```

Message：

```text
Advance independent Engine Forward through 2026-07-22
```

结果：

```text
521 files changed
14642 insertions
383 deletions

新增：
Engine automation/current_run.json
2026-07-20 daily artifacts
2026-07-21 daily artifacts
2026-07-22 daily artifacts

更新：
data/fw_prices
runtime/current
runtime/history/orders.jsonl
runtime/history/equity_curve.json
```

最终：

```text
HEAD == origin/main
TRUE
```

---

# 8. Engine Forward 正式时间线

Canonical 5Y 最后常规 EOD：

```text
2026-06-16
```

Seed：

```text
seed_2026-06-16
```

首次 Forward 日期：

```text
2026-06-17
```

初始日期序列：

```text
2026-06-17
2026-06-18
2026-06-22
```

说明：

```text
2026-06-19 不属于完整共同交易日序列。
```

Formal catch-up：

```text
2026-06-17 → 2026-07-17
21 个交易日
```

首次 Daily incremental：

```text
2026-07-20
2026-07-21
2026-07-22
```

当前：

```text
last_committed_date = 2026-07-22
equity curve points = 24
```

---

# 9. 当前 Engine Forward 状态

当前账户日期：

```text
2026-07-22
```

当前最后已知权益：

```text
278130.3458399169
```

Engine Forward 独立权益曲线：

```text
exports/official/
FD-M3180125-SP500-TOP3-engine/
forward/runtime/history/equity_curve.json
```

当前点数：

```text
24
```

最后三个日期：

```text
2026-07-20
2026-07-21
2026-07-22
```

正式 Daily 运行具备：

```text
从 runtime/current 恢复
只规划 last_committed_date 之后日期
逐日 commit
更新 current
更新 daily
更新 orders
更新 Engine equity curve
无新日期时 result_count = 0
幂等
```

---

# 10. GitHub Actions 当前状态

现有 Legacy workflow：

```text
每日更新 S&P 500 数据
```

最新截图确认：

```text
Legacy workflow run #115 overall succeeded
```

Legacy OOS 步骤中出现：

```text
ERROR_OOS_STALE:
market data=2026-07-22
expected=2026-07-23
```

该错误属于 Legacy OOS 自身日期判断，不属于 Engine Forward。

Engine 独立 workflow：

```text
Engine Forward Daily
```

当前截图状态：

```text
workflow 已出现在 Actions 列表
尚无运行记录
```

合理原因：

```text
新 workflow 安装发生在最近一次 Legacy workflow 完成之后；
workflow_run 不会追溯触发已经结束的历史运行。
```

当前决定：

```text
不手动制造当天未收盘数据；
等待下一次美股收盘后的 Legacy workflow 正常完成；
观察 workflow_run 是否自动触发 Engine Forward Daily。
```

观察验收：

```text
1. Engine Forward Daily 自动出现运行记录；
2. workflow 全部步骤成功；
3. 若有新完整交易日：
   - 新增 runtime/daily/YYYY-MM-DD
   - runtime/current 同步推进
   - Engine equity curve 延长
   - automation/current_run 更新
4. 若无新完整日期：
   - result_count = 0
   - 正常成功退出
5. Legacy 数据与 Legacy equity curve 不变。
```

---

# 11. 今日完成与未完成

## 11.1 完成

```text
Canonical Forward Contract Freeze
Read-only preflight
Formal Forward catch-up
Runtime acceptance
21-day official runtime creation
Independent Engine Forward workflow installation
Independent Engine Forward runner installation
Parallel isolation contract
First official incremental daily run
2026-07-20 / 21 / 22 committed
Independent equity curve advanced to 24 points
Idempotent rerun PASS
Legacy workflow unchanged
Legacy Forward unchanged
Legacy equity curve unchanged
Git push complete
```

## 11.2 未完成

```text
下一次真实 workflow_run 自动触发观察
Dashboard manifest
Dashboard Engine data source
Dashboard Engine equity curve
E2E
Legacy system retirement
Final cutover
```

---

# 12. 下一步唯一入口

在任何下一步前，必须先：

```text
回看当前对话历史
回看最近 3 天 Daily Log
回看 2026-07-23 本日志
```

当前不需要新增代码。

下一步唯一观察事项：

```text
OBSERVE_NEXT_AUTOMATIC_GITHUB_DAILY_RUN
```

等待下一次美股收盘后：

```text
Legacy 每日更新完成
→ workflow_run
→ Engine Forward Daily
→ data/fw_prices
→ Engine Shared Runtime
→ Engine official daily artifacts
→ Engine independent equity curve
→ commit / push
```

若自动 workflow 运行成功：

```text
FW-Step 2 自动生产运行事实确认完成
```

若自动 workflow 失败：

```text
先回看历史和最近 3 天日志
再读取完整 Actions 日志
进行 FACT / INFERENCE / UNKNOWN 分类
取得用户确认后才输出修复代码
```

---

# 13. 今日最终判定

```text
Overall Step 1:
COMPLETE / PASS

Overall Step 2:
IN_PROGRESS
Engine Forward GitHub Daily Production:
ACTIVE

FW-Step 2:
PRODUCTION ACTIVE
Pending one automatic workflow_run observation

FW-Step 3 Dashboard:
NOT_STARTED

Legacy / Engine separation:
PASS

Engine supplies legacy equity curve:
FALSE

Engine supplies current Dashboard:
FALSE

Future cutover:
NOT PERFORMED

Current HEAD:
1d3bf156e6dd674ea7278934cc39a8da547d42e2

HEAD == origin/main:
TRUE
```

---

# 14. 收工结论

今天的核心成果不是 Dashboard，也不是替换 Legacy 系统，而是：

```text
Engine Forward 已经成为一套独立、正式、可连续运行的 GitHub Forward 系统。
```

它已经能够：

```text
独立读取 Forward 数据
独立恢复账户状态
独立逐日运行
独立生成 daily artifacts
独立维护订单历史
独立维护 equity curve
独立 commit / push
无新日期时幂等退出
```

同时：

```text
Legacy GitHub Forward 仍保持原样运行；
Legacy equity curve 未被 Engine 数据覆盖或供应；
Dashboard 尚未接入 Engine Forward；
最终切换留待 FW-Step 3。
```

后续所有开发必须遵守：

```text
有问题和下一步骤前先回看历史及最近 3 天日志；
得到确认后再输出代码。
```
---

# 15. 日志归档状态与 Git 语义

本日志在生成时引用的项目代码与 Engine Forward 产物 HEAD 为：

```text
PROJECT_HEAD_AT_LOG_CREATION =
1d3bf156e6dd674ea7278934cc39a8da547d42e2
```

该 HEAD 对应：

```text
Advance independent Engine Forward through 2026-07-22
```

需要严格区分：

```text
PROJECT_HEAD_AT_LOG_CREATION
!=
DAILY_LOG_ARCHIVE_COMMIT
```

在本日志尚未进入仓库前：

```text
DAILY_LOG_REPOSITORY_STATUS = NOT_YET_COMMITTED
```

本次归档脚本运行后，日志将写入仓库根目录：

```text
2026-07-23_SP500_E1R_DAILY_LOG.md
```

并以独立白名单提交完成归档。最终归档 commit 以脚本运行结果为准，不在日志正文中预先猜测。

---

# 16. GitHub 自动化部署状态冻结

截至本日志归档前，已经证实：

```text
Workflow installed in GitHub:
TRUE

Independent runner installed in GitHub:
TRUE

Local production-path official run:
PASS

First incremental Engine Forward run:
PASS

GitHub automatic workflow_run observed:
FALSE

GitHub Actions automatic Engine commit/push observed:
FALSE
```

因此当前准确部署状态为：

```text
FW_STEP2_DEPLOYMENT_STATE =
ACTIVE_PENDING_FIRST_AUTOMATIC_WORKFLOW_RUN_OBSERVATION
```

这不代表 Engine Forward 不能运行；它已经通过本地正式生产路径产生并提交了新数据。尚缺的只是 GitHub Actions 环境中的第一次自动触发事实。

---

# 17. Engine Forward Daily Workflow 正式运行合同

独立 workflow：

```text
.github/workflows/engine-forward-daily.yml
name: Engine Forward Daily
```

触发：

```text
workflow_dispatch
或
workflow_run:
  在 “每日更新 S&P 500 数据” workflow completed 后触发
```

执行条件：

```text
手动 workflow_dispatch
或
上游 Legacy workflow conclusion == success
```

权限：

```text
contents: write
```

并发合同：

```text
group = engine-forward-daily
cancel-in-progress = false
```

正式执行链：

```text
Legacy workflow 完成
→ Engine Forward Daily
→ checkout main
→ Python 3.11
→ pip install -e .
→ scripts/run_engine_forward_daily.py
→ data/prices 同步至 data/fw_prices
→ Shared Runtime / Engine
→ Engine official runtime
→ Engine-only staging
→ commit
→ pull --rebase
→ push
```

## 17.1 下一次 GitHub 自动运行观察风险

下一次实际自动运行需要验证：

```text
1. workflow_run 对中文 workflow 名称匹配正确；
2. GITHUB_TOKEN 具有 contents: write 权限；
3. Engine workflow 能在 GitHub Actions 环境完成 pip install -e .；
4. Legacy workflow push 与 Engine workflow push 不发生不可恢复冲突；
5. git pull --rebase origin main 能处理正常的上游更新；
6. 有新完整交易日时正确新增 Engine daily artifacts；
7. 没有新完整交易日时 result_count = 0 并正常成功退出；
8. Engine-only staging 不包含 Legacy 路径；
9. Legacy Forward 和 Legacy equity curve 保持不变。
```

这些是对已部署自动化的运行观察标准，不是新的开发阶段。

---

# 18. 2026-07-22 Engine Forward 账户基线

本节中的精确数值由日志归档脚本直接读取以下正式文件后写入：

```text
exports/official/
FD-M3180125-SP500-TOP3-engine/
forward/runtime/current/account_state.json

exports/official/
FD-M3180125-SP500-TOP3-engine/
forward/runtime/current/runtime_state.json

exports/official/
FD-M3180125-SP500-TOP3-engine/
forward/runtime/daily/2026-07-20/

exports/official/
FD-M3180125-SP500-TOP3-engine/
forward/runtime/daily/2026-07-21/

exports/official/
FD-M3180125-SP500-TOP3-engine/
forward/runtime/daily/2026-07-22/
```

动态归档内容包括：

```text
cash
positions_value
total_equity
持仓代码
每只 quantity
avg_cost
last_price
market_value
unrealized_pnl
pending_orders
closed_trades
07-20 / 07-21 / 07-22 的 order_intent 数量
07-20 / 07-21 / 07-22 的 fill 数量
```

禁止凭推断填写；必须以正式 JSON 为准。

<!-- DYNAMIC_ACCOUNT_SNAPSHOT_START -->
```text
account_date = 2026-07-22
cash = 70153.97
positions_value = 207976.3758399169
total_equity = 278130.3458399169
open_positions_count = 3
position_symbols = ['DELL', 'HUM', 'MRVL']
DELL:
  quantity = 156.48065098333814
  avg_cost = 219.21899999999997
  last_price = 441.8
  market_value = 69133.1516044388
  unrealized_pnl = 34829.61977652239
HUM:
  quantity = 240.76122161662295
  avg_cost = 373.07269999999994
  last_price = 397.6
  market_value = 95726.66171476929
  unrealized_pnl = 5905.222710957416
MRVL:
  quantity = 204.35358320635484
  avg_cost = 168.02786
  last_price = 210.99
  market_value = 43116.56252070881
  unrealized_pnl = 8779.467251213067
pending_order_count = 0
closed_trade_count = 0
2026-07-20: order_intents=3, fills=0
2026-07-21: order_intents=3, fills=0
2026-07-22: order_intents=3, fills=0
```
<!-- DYNAMIC_ACCOUNT_SNAPSHOT_END -->

---

# 19. 最新输入与正式产物 Hash 基线

本次首次独立 Daily 运行确认：

```text
data/prices source file count = 498
data/fw_prices copied_or_updated = 494
data/fw_prices unchanged = 4
latest Engine complete date = 2026-07-22
```

归档脚本将计算并写入以下 Hash：

```text
data/fw_prices file count
runtime/current/runtime_state.json SHA256
runtime/current/account_state.json SHA256
runtime/history/equity_curve.json SHA256
automation/current_run.json SHA256
.github/workflows/engine-forward-daily.yml SHA256
scripts/run_engine_forward_daily.py SHA256
```

这些 Hash 用于下一次自动运行前后的连续性核验，不得替代 Runtime 自身的日期与合同验证。

<!-- DYNAMIC_HASH_BASELINE_START -->
```text
data/fw_prices file count = 498
exports/official/FD-M3180125-SP500-TOP3-engine/forward/runtime/current/runtime_state.json SHA256 = 8a72420ef46802b54a4152d2821bc95d4d353164110fd3b92ecb1a42ca4b92be
exports/official/FD-M3180125-SP500-TOP3-engine/forward/runtime/current/account_state.json SHA256 = c86cd17ede0b482b40f4b49c44e530a525f6847a9afdd0b10eb2bb4b028fed60
exports/official/FD-M3180125-SP500-TOP3-engine/forward/runtime/history/equity_curve.json SHA256 = 516ff8b51ace99c4b9fcfbe182d2ebf7bbfdb326e1cf12f5128c8a95b67ddf6d
exports/official/FD-M3180125-SP500-TOP3-engine/forward/automation/current_run.json SHA256 = c9f1f4eae707045ed59d0f3aecfb8246d8880e8cd4f8a5079c371b1cb1066fc9
.github/workflows/engine-forward-daily.yml SHA256 = 614b57e92f3772f3120cb11cf2e5f54ab606e6f5911da23793709dc3c56e01d3
scripts/run_engine_forward_daily.py SHA256 = 2ed42b95a471e4fc63f369d7dc22890802e0711f59a45465285ef693b6944672
```
<!-- DYNAMIC_HASH_BASELINE_END -->

---

# 20. 历史日期口径覆盖声明

以下旧历史说法正式标记为：

```text
SUPERSEDED / NON-CANONICAL
```

旧说法：

```text
Seed boundary = 2026-06-18
First Forward date = 2026-06-22
```

当前唯一 Canonical 合同：

```text
Seed date = 2026-06-16
First Forward market date = 2026-06-17
Initial sequence =
2026-06-17
2026-06-18
2026-06-22
```

原因：

```text
旧说法属于当时证据尚未完整时的临时边界判断；
已被 Canonical Forward Contract Freeze、
正式 Seed reconciliation、
Formal Catch-up 和 Runtime Acceptance 覆盖。
```

后续回看最近三天日志时，如遇旧口径，必须以本节和正式 Contract Freeze 为准，不得重新讨论已经覆盖的日期边界。

---

# 21. 本日志的权威使用方式

下一次出现问题或准备提出步骤时，必须依次读取：

```text
1. 当前对话相关历史；
2. 最近 3 天 Daily Log；
3. 本日志；
4. 相关 Contract Freeze / Architecture / Handoff；
5. 当前 GitHub Runtime 和 Actions 证据。
```

然后输出：

```text
FACT
INFERENCE
UNKNOWN
```

在用户确认目标、范围、约束和验收标准前，不得输出实现代码或可执行脚本。

本日志完成 GitHub 归档后，将成为 2026-07-23 工作日的正式权威入口。
